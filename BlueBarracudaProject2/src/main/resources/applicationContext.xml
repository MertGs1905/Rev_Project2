<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
	
	<context:annotation-config/>
	 <context:component-scan base-package="*"/>
<!-- 	 <tx:annotation-driven/> -->
<!-- 	 <mvc:annotation-driven/> -->
		
		
	
	<!-- DATASOURCE This bean is a JDBC instance. -->
	<bean id="dataSource"
		class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="oracle.jdbc.driver.OracleDriver" />
		<property name="url"
			value="jdbc:oracle:thin:@revychan.c4wt8faaxlgp.us-east-2.rds.amazonaws.com:1521:orcl" />
		<property name="username" value="pokemondb" />
		<property name="password" value="p4ssw0rd" />
	</bean>

	<!-- SESSION FACTORY This bean is a Hibernate instance. -->
	<bean id="sessionFactory"
		class="org.springframework.orm.hibernate5.LocalSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="packagesToScan" value="model" />
		<property name="hibernateProperties">
			<props>
				<prop key="hibernate.dialect">org.hibernate.dialect.Oracle10gDialect</prop>
				<prop key="hibernate.show_sql">false</prop>
				<prop key="hibernate.format_sql">false</prop>
				<prop key="hibernate.hbm2ddl.auto">none</prop>
			</props>
		</property>
	</bean>

<!-- 	TRANSACTION MANAGER This bean is a Hibernate instance. This bean is  -->
<!-- 		responsible for managing your transaction (big surprise) and ALSO managing  -->
<!-- 		your contextual sessions. What are contextual sessions? Sessions with information  -->
<!-- 		specific to that user. They make it so that each user sees only THEIR information. -->
	<bean id="transactionManager"
		class="org.springframework.orm.hibernate5.HibernateTransactionManager">
		<property name="sessionFactory" ref="sessionFactory" />
	</bean>
</beans>
	


